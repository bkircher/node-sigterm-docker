name: Test
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Build and run
        run: |
          docker-compose build
          docker-compose up -d

      - name: Send signal
        run: |
          docker kill -s SIGTERM node
          docker-compose down
